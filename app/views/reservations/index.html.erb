<div class="container">
  <h1>
    <% if @user.first_name == nil %>
      <%="Bienvenue #{@user.email}"%>
    <% else %>
      <%="Bienvenue #{@user.first_name}"%>
    <% end %>
  </h1>
</div>
<div class="container">
  <h2>
    <% if @user.reservations.count == 0 %>
      <%="Pas (encore) de salles de classes réservées"%>
    <% else %>
      <%="Salles de classe réservées"%>
    <% end %>
  </h2>
  <ul class="list-group">
    <% @user.reservations.each do |reservation| %>
      <li class="list-group-item">
        Réservation à <strong><%= link_to reservation.space.city.capitalize, reservation_path(reservation) %></strong> | Réservation : 000<%= reservation.id %>
      </li>
    <% end %>
  </ul>
</div>
<div class="container">
  <h2>
    <% if @user.spaces.count == 0 %>
      <%="Pas (encore) de salles de classes louées"%>
    <% else %>
      <%="Salles de classe louées"%>
    <% end %>
  </h2>
  <ul class="list-group">
    <% @user.spaces.each do |space|  %>
      <li class="list-group-item">
        Louée à <strong><%= link_to space.city.capitalize,space_path(space) %></strong> | Espace : 000<%= space.id %>
      </li>
    <% end %>
  </ul>
</div>
